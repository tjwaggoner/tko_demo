# TKO Brick-Con FY27: Agentic Fraud Triage Demo

**Live App:** https://tko-fraud-triage-7474655348825387.aws.databricksapps.com

## What I Did (5 Bullets)

1. Gave Claude Code a single prompt with the Brick-Con Google Doc and Slack channel — it read the use case requirements, understood the context, and planned the entire build autonomously.
2. Claude generated 2,000 realistic mock banking transactions and 1,500 login events with fraud patterns, then created a Unity Catalog catalog, schema, Volume, and Delta tables — all via CLI, no notebooks.
3. Built a full-stack Databricks App (FastAPI + JavaScript) with a live fraud investigation queue — analysts see flagged transactions ranked by risk score and can Block, Clear, or Escalate with one click.
4. Integrated Foundation Model API so investigators can click "AI Explain" on any transaction and get a plain-English risk assessment generated by Claude Sonnet in real time.
5. Deployed the entire demo — from empty directory to live app — in under 15 minutes using Claude Code, FE Vibe skills, and MCP integrations with zero manual coding.

## Databricks Products Used

| Product | How It's Used |
|---|---|
| **Unity Catalog** | `ticket_master.fraud_detection` schema with governed tables and managed Volume for raw data lineage |
| **Unity Catalog Volumes** | `source_data` volume stores raw CSV files uploaded from local generation |
| **Delta Tables** | `transactions` (2,000 rows), `login_logs` (1,500 rows), `triage_actions` (write-back for analyst decisions) |
| **Serverless SQL Warehouse** | All queries run on the Serverless Starter Warehouse — no cluster management |
| **Databricks Apps** | Full-stack app deployed natively — FastAPI backend + HTML/JS frontend, OAuth handled automatically |
| **Foundation Model API** | `databricks-claude-sonnet-4` serves AI-powered risk explanations via OpenAI-compatible endpoint |
| **Databricks CLI** | Catalog creation, schema setup, volume management, file upload, app creation and deployment |
| **FE Vending Machine (FEVM)** | Serverless workspace provisioned and authenticated via the FEVM skill |

## Claude Code Skills Used

| Skill | What It Did |
|---|---|
| **`fe-databricks-tools:databricks-authentication`** | Verified and configured Databricks CLI profile for FEVM serverless workspace |
| **`fe-databricks-tools:databricks-fe-vm-workspace-deployment`** | Provisioned FEVM serverless workspace, managed session auth via Chrome DevTools |
| **`fe-databricks-tools:databricks-data-generation`** | Informed realistic mock data patterns for banking transactions and login logs |
| **`fe-workflows:slack-discovery-agent`** | Retrieved context from `#ext-tko-amer-fy27` Slack channel to understand the event |
| **`fe-google-tools:google-drive`** | Read the TKO Brick-Con Brief Google Doc to extract use case requirements |

## MCP Integrations Used

| MCP Server | What It Did |
|---|---|
| **Google MCP** | Extracted the full Brick-Con use case doc with all tabs and requirements |
| **Slack MCP** | Pulled recent messages from the TKO channel for Day 3 build context |
| **Chrome DevTools MCP** | Automated FEVM SSO authentication and session cookie capture |

## Architecture

```
┌─────────────────────────────────────────────────────────┐
│                   Databricks App                        │
│              (FastAPI + HTML/JS Frontend)                │
│                                                         │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ Fraud    │  │ AI Explain   │  │ Triage Actions    │  │
│  │ Queue UI │  │ (FMAPI)      │  │ (Block/Clear/Esc) │  │
│  └────┬─────┘  └──────┬───────┘  └────────┬──────────┘  │
│       │               │                   │              │
└───────┼───────────────┼───────────────────┼──────────────┘
        │               │                   │
        ▼               ▼                   ▼
┌──────────────┐ ┌─────────────┐  ┌─────────────────────┐
│ Serverless   │ │ Foundation  │  │ Delta Table         │
│ SQL Warehouse│ │ Model API   │  │ triage_actions      │
│              │ │ (Claude     │  │ (MERGE write-back)  │
│              │ │  Sonnet)    │  │                     │
└──────┬───────┘ └─────────────┘  └─────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│ Unity Catalog                        │
│ ticket_master                        │
│  └─ fraud_detection                  │
│      ├─ transactions (2,000 rows)    │
│      ├─ login_logs (1,500 rows)      │
│      ├─ triage_actions               │
│      └─ [Volume] source_data/        │
│           ├─ transactions.csv        │
│           └─ login_logs.csv          │
└──────────────────────────────────────┘
```

## Mock Data Summary

**Transactions** — 2,000 banking transactions with ~12% fraud rate
- Legitimate: everyday merchants (Amazon, Starbucks, Shell Gas) with $5-$500 amounts
- Suspicious: wire transfers (Cayman Islands, Nigeria), crypto exchanges, luxury goods with $2,000-$50,000 amounts
- Flag reasons: velocity_spike, geo_anomaly, amount_threshold, device_mismatch, mfa_change, new_merchant, cross_channel

**Login Logs** — 1,500 login events with ~8% suspicious activity
- Suspicious logins: new device fingerprints, foreign geolocations (Lagos, Moscow, Bucharest), MFA changes, high failed attempt counts

## Project Structure

```
tko_demo/
├── app.py              # FastAPI backend + HTML frontend
├── app.yaml            # Databricks App config
├── databricks.yml      # Bundle config
├── requirements.txt    # Python dependencies
├── README.md
├── data/
│   ├── transactions.csv
│   └── login_logs.csv
└── scripts/
    └── generate_data.py  # Mock data generator
```

## Built With

- **Claude Code (Opus 4.6)** — End-to-end build orchestration, zero manual coding
- **FE Vibe** — Skills and sub-agents for Databricks, Slack, Google, and Chrome automation
- **Databricks FEVM** — Serverless workspace runtime
- **FastAPI + Uvicorn** — App backend
- **Databricks SDK** — SQL execution, workspace client
- **OpenAI SDK** — Foundation Model API integration
